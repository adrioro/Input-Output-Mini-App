myApp = {}; //First we create an app object.

//Getting the elements:

myApp.selectElement = function (element) {
    return document.querySelector(element);
}

var myInp = document.querySelector("#entry");
var myOut = document.querySelector("#output");

myApp.onkeyup = function () {
    a = myInp.value = myOut.innerText;
    (#output) innerText.write.myInp
}